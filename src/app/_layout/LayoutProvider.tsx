import { type ReactNode } from 'react';


/**
 * LayoutProvider Component:  Descripción del comportamiento...
 */
export function LayoutProvider({ children }: { children: ReactNode }) {
  // -----------------------CONSTS, HOOKS, STATES
  // -----------------------MAIN METHODS
  // -----------------------AUX METHODS
  // -----------------------RENDER
  return (
    <>
      <main>{children}</main>
    </>
  );
}
